import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import "./globals.css"

const geistSans = Geist({
  subsets: ["latin"],
  variable: "--font-geist-sans",
})

const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
})

export const metadata: Metadata = {
  title: "claim-resolve — Multi-agent claims reconciliation",
  description:
    "A multi-agent pipeline that reconciles insurance claims across carriers, and shows exactly how and why it made each call. Cross-carrier entity resolution with explainable, auditable AI.",
  generator: "v0.app",
}

export const viewport = {
  themeColor: "#0d1117",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} bg-background`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
